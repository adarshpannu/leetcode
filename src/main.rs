#[allow(warnings)]

mod lc564_closest_palindrome;

fn main() {
    println!("Hello, world!");
}

use itertools::Itertools;

/* */
#[test]
fn test() {
    let l = vec![3, 3, 3, 2, 1, 3, 2];

    let x = l.iter().zip(0..l.len()).group_by(key)
}

*/
